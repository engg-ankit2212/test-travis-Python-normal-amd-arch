os: linux
language: python

jobs:
  include:
    - dist: focal
      arch: amd64
      group: dev
      script:
        - python hello_world.py
    - dist: bionic
      arch: amd64
      group: dev
      script:
        - python hello_world.py
    - dist: xenial
      arch: amd64
      group: dev
      script:
        - python hello_world.py
